import { VirtualKeyboardDirective } from './virtual-keyboard.directive';

describe('VirtualKeyboardDirective', () => {
  it('should create an instance', () => {
    const directive = new VirtualKeyboardDirective();
    expect(directive).toBeTruthy();
  });
});
